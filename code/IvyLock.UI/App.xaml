<Application
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:IvyLock.UI"
             xmlns:view="clr-namespace:IvyLock.UI.View"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="IvyLock.UI.App"
             StartupUri="View/SettingsView.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="BackgroundDark" Color="#000000" />
        <SolidColorBrush x:Key="BackgroundLight" Color="#1A1A1A" />
        <SolidColorBrush x:Key="Foreground" Color="#FFFFFF" />
        <SolidColorBrush x:Key="Control" Color="#292929" />
        <SolidColorBrush x:Key="Highlight" Color="#383838" />

        <Style TargetType="{x:Type Window}">
            <Setter Property="Foreground" Value="{DynamicResource Foreground}" />
            <Setter Property="FontFamily" Value="Roboto" />
        </Style>
        <Style TargetType="{x:Type PasswordBox}">
            <Setter Property="MinWidth" Value="192" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Foreground}" />
            <!--<Setter Property="Template"  Value="{StaticResource PasswordBoxControlTemplate}" />-->
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="MinWidth" Value="256" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Background" Value="{DynamicResource Control}" />
            <Setter Property="Foreground" Value="{DynamicResource Foreground}" />
            <Setter Property="Template"  Value="{StaticResource PasswordBoxControlTemplate}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <ControlTemplate x:Key="PasswordBoxControlTemplate" TargetType="{x:Type PasswordBox}">
            <ControlTemplate.Resources>
                <Storyboard x:Key="HasNoPassword">
                    <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.Y)" Storyboard.TargetName="textBlock">
                        <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="0" />
                    </DoubleAnimationUsingKeyFrames>
                </Storyboard>
                <Storyboard x:Key="HasPassword">
                    <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.Y)" Storyboard.TargetName="textBlock">
                        <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="-16" />
                    </DoubleAnimationUsingKeyFrames>
                    <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(TextElement.FontSize)" Storyboard.TargetName="PART_ContentHost">
                        <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="10" />
                    </DoubleAnimationUsingKeyFrames>
                </Storyboard>
            </ControlTemplate.Resources>
            <Border x:Name="border"
                    BorderBrush="{DynamicResource Highlight}"
                    BorderThickness="0,0,0,2"
                    Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True"
                    Background="{TemplateBinding Background}">
                <Grid>
                    <TextBlock x:Name="textBlock" Text="{TemplateBinding view:IvyLock.Placeholder}"
                               FontWeight="SemiBold"  Opacity="1"
                               HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                               FontSize="10" RenderTransformOrigin="0.5,0.5"
                               VerticalAlignment="Center" Margin="0">
                        <TextBlock.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform />
                                <SkewTransform />
                                <RotateTransform />
                                <TranslateTransform Y="-16" />
                            </TransformGroup>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                    <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" VerticalAlignment="Center" />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="view:IvyLock.PasswordLength" Value="0">
                    <Setter Property="RenderTransform" TargetName="textBlock">
                        <Setter.Value>
                            <TransformGroup>
                                <ScaleTransform />
                                <SkewTransform />
                                <RotateTransform />
                                <TranslateTransform Y="-16" />
                            </TransformGroup>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="FontSize" TargetName="textBlock" Value="10" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" TargetName="border" Value="0.56" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource Foreground}" />
                </Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource Foreground}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Application.Resources>
</Application>